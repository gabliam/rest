/** Type in registry */
export const TYPE = {
    Controller: Symbol('Controller'),
};

export const METADATA_KEY = {
    controller: '_controller',
    controllerMethod: '_controller-method',
};

export const DEFAULT_ROUTING_ROOT_PATH = '/';

export const REST_CONFIG = Symbol('REST_CONFIG');